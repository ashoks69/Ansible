- name: Install and run nginx
  hosts: web
  become: yes # -b == Root user
  tasks:
    - name: install nginx
      ansible.builtin.package:
        name: nginx
        state: present
    - name: start nginx
      ansible.builtin.package:
        name: nginx
        state: installed
        status: yes
