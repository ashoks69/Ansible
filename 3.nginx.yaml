- name: Install and start Ngnix
  hosts: web
  become: yes # -b == root user
  tasks: 
  - name: Install Ngnix
    ansible.builtin.package:
      name: nginx
      state: present
  - name: Start Ngnix
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes
  
