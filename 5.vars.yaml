- name: varible
  hosts: local
  connection: local
  vars:
    -vars
  tasks:
    - name: using variables
      ansible.builtin.debug:
        msg: "My slef {{ name }}, working as {{ designation }} in {{ Org }}"